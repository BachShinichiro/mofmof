<%= form_with(model: station, local: true) do |form| %>
  <% if station.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(station.errors.count, "error") %> prohibited this station from being saved:</h2>

      <ul>
      <% station.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :line_name %>
    <%= form.text_area :line_name %>
  </div>

  <div class="field">
    <%= form.label :station_name %>
    <%= form.text_area :station_name %>
  </div>

  <div class="field">
    <%= form.label :minuites_on_foot %>
    <%= form.number_field :minuites_on_foot %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
